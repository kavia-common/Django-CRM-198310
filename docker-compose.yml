version: "3.9"

services:
  db:
    image: postgres:16
    # NOTE: This env file is intentionally separate from backend/.env:
    # - Postgres expects POSTGRES_* variables.
    # - Django expects DB* variables (DBNAME, DBUSER, DBPASSWORD, DBHOST, DBPORT).
    env_file:
      - ./db.env
    # volumes:
    #   - /home/db:/var/lib/postgresql/data
    healthcheck:
      # Use POSTGRES_* here because those are the variables provided to the db container.
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB -h 127.0.0.1 -p 5432"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s

  redis:
    image: redis:7
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  web:
    build:
      context: .
      args:
        # Dockerfile currently requires APP_NAME for cache/test; keep it stable.
        APP_NAME: Django-CRM-198310
    image: micropyramid/django-crm:1
    env_file:
      - ./backend/.env
    ports:
      - "8001:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Run migrations on container boot for cold-start readiness.
      RUN_MIGRATIONS: "true"
      # In-container bind target
      HOST: "0.0.0.0"
      # Ensure container-to-container DB host is the compose service name (not localhost).
      DBHOST: "db"
    command: ["python", "/app/backend/manage.py", "runserver", "0.0.0.0:8000"]
    healthcheck:
      # Use existing Django template endpoint to avoid changing API contracts.
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8000/healthz/ >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 20
      start_period: 20s

  celery:
    build:
      context: .
      args:
        APP_NAME: Django-CRM-198310
    image: micropyramid/django-crm:1
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      # Enforce requested ordering: postgres -> web -> celery
      web:
        condition: service_healthy
    environment:
      # Celery should not run migrations.
      RUN_MIGRATIONS: "false"
      # Ensure container-to-container DB host is the compose service name (not localhost).
      DBHOST: "db"
    command: ["celery", "-A", "crm", "worker", "--loglevel=INFO"]
    healthcheck:
      # Minimal health signal: ensure the celery process exists in the container.
      # (We avoid requiring extra celery ping configuration and broker auth here.)
      test: ["CMD-SHELL", "ps aux | grep -q '[c]elery\\s\\+-A\\s\\+crm\\s\\+worker'"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s
